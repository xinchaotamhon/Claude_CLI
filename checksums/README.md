# Binary checksum records

`claude.exe.sha256` is generated from the binary currently stored at
`bin/claude.exe`. After an approved binary replacement, regenerate the hash,
record the source/version/architecture in dated evidence, and rerun the smoke
gates. Do not put credentials or session data in this directory.
